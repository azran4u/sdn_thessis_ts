// import _ from 'lodash';
// import { Range } from '../config';
// import { NetworkNode } from '../model/networkNode';
// import { EdgeLabel } from '../model/edgeLabel';
// import { Edge, Graph } from 'graphlib';

// export class GraphlibLabels {

//     static getNodeLabel(graph: Graph, node: string): NetworkNode {
//         return graph.node(node) as any as NetworkNode;
//     }

//     static getEdgeLabel(graph: Graph, edge: Edge): EdgeLabel {
//         return graph.edge(edge) as any as EdgeLabel;
//     }
// }